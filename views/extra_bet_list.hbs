{{#> layout}}
{{#*inline "head"}}
<title>{{ @t "extra_bet_list.title" }} - {{@websiteName}}</title>
<style>
    .highlight-friend {
        background-color: #F8DE7E;
        padding: 3px 5px;
        display:inline-block;
        border-radius: 3px;
    }
    .highlight-me {
        background-color: #ffd700;
        padding: 3px 5px;
        display:inline-block;
        border-radius: 3px;
    }
</style>
{{/inline}}
{{#*inline "body"}}
{{> navbar}}
<div class="container">

{{#each extraBets}}
    <div class="card mb-3">
        <div class="card-header">
            {{ name }} 
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>{{ @t "extra_bet_list.team_column" }}</th>
                    <th>{{ @t "extra_bet_list.users_column" }}</th>
                </tr>
            </thead>
            <tbody>
                {{#each teams }}
                <tr>
                    <td>
                        {{#if team.code}}<img class="flag-icon me-1" src="{{flagUrl team.code}}" alt="{{team.name}} flag" loading="lazy">{{/if}}{{ team.name }}
                        ({{ userCount }})
                    </td>
                    <td>
                        {{#each users }}
                            {{#if @first}}{{else}}, {{/if}}
                            <span {{#if isFriend}}class="highlight-friend"{{/if}}{{#if isMe}}class="highlight-me"{{/if}}>
                                <a href="/user/{{id}}">
                                    {{ name }}
                                </a>
                            </span>
                        {{/each}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
{{else}}
    <div class="alert alert-info">
        {{ @t "extra_bet_list.empty_state" }}
    </div>
{{/each}}

</div>
{{/inline}}
{{#*inline "scripts"}}

{{/inline}}
{{/layout}}
