{{#> layout}}
{{#*inline "head"}}
<title>{{displayedUser.name}} - {{@websiteName}}</title>
<style>
.result-correct {
    background-color: #009900;
}
.result-diff {
    background-color: #00e600;
}
.result-winner {
    background-color: #ccffcc;
}
</style>
{{/inline}}
{{#*inline "body"}}
{{> navbar}}
<div class="container">

<h3>{{displayedUser.name}} <small class="text-muted">Score {{displayedUser.score}}, Ranking {{displayedUser.rank}}</small></h3>

{{#if matches}}
<table class="table table-sm">
    <thead>
    <tr>
        <th colspan="2"></th>
        <th colspan="3" class="text-center">Match Result</th>
        <th class="text-center">Bet</th>
        <th class="text-center">Points</th>
    </tr>
    </thead>
    {{#each matches}}
    <tr class="result-{{dataValues.result}}">
        <td class="small"><span class="d-none d-sm-inline">{{calendar when}}</span><span class="d-sm-none">{{calendarShort when}}</span></td>
        <td class="small"><span class="d-none d-sm-inline">{{MatchType.name}}</span><span class="d-sm-none">{{MatchType.code}}</span> {{> score_factor MatchType.scoreFactor}}</td>
        <td class="text-right">
            <em class="d-none d-sm-inline">{{HomeTeam.name}}</em><em class="d-sm-none">{{HomeTeam.code}}</em>
        </td>
        <td class="text-center">
            <strong>{{showGoals goalsHome}}</strong> : <strong>{{showGoals goalsAway}}</strong>
        </td>
        <td class="text-left">
            <em class="d-none d-sm-inline">{{AwayTeam.name}}</em><em class="d-sm-none">{{AwayTeam.code}}</em>
        </td>
        <td class="text-center">
        {{showGoals Bets.0.goalsHome}} : {{showGoals Bets.0.goalsAway}}
        </td>
        <td class="text-center {{#if (isZero dataValues.score)}}is-zero{{/if}}">
        {{dataValues.score}}
        </td>
    </tr>
    {{/each}}
</table>
{{else}}
<div class="alert alert-danger" role="alert">
    <strong>There are no bets to show.</strong>
</div>
{{/if}}
</div>
{{/inline}}
{{/layout}}
